# Image versions for this release of the CARE Clinic app.
# The care engine reads this (and docker-compose.yml reads the exported vars).
#
# TODO: pin a known-good release so every package is reproducible — set
#   BACKEND_IMAGE / FRONTEND_IMAGE  (built tags) and
#   CARE_BE_REF / CARE_FE_REF       (git refs the engine clones + builds).
# For now the engine clones ohcnetwork/care + ohcnetwork/care_fe at `develop` and
# builds both locally (BACKEND_IMAGE=care:clinic, FRONTEND_IMAGE=care_fe:clinic).
